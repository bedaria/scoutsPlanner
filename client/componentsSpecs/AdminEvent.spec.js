import React from 'react'
import { expect } from 'chai'
import { shallow, render } from 'enzyme'
import AdminEvent from '../components/AdminEvent'

describe('<AdminEvent />', () => {
  it('should display the total number of people invited', () => {})
  it('should display the total number of Maybes', () => {})
  it('should display the total number of Yess', () => {})
  it('should display the total number of Nos', () => {})
  it('should have a button for editing event', () => {})
  it('should display time blocks for the event', () => {})
  it('should have a button to ask people again', () => {})



})
